node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
# The static SPA build under /build is generated by `pnpm build` / `make web`
# and embedded via go:embed. It is intentionally NOT committed; the root
# .gitignore ignores it (keeping a .gitkeep so go:embed works in a fresh
# checkout). Rebuild with `make web` / `make build` / the Dockerfile.

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
