node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
# The static SPA build under /build is committed (ADR 0015) so `go build`
# alone produces a binary with the embedded UI; do not ignore it.

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
